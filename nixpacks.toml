[phases.setup]
nixPkgs = ["nodejs"]

[phases.build]
cmds = [
  "composer install --no-dev",
  "npm install",
  "npm run build"
]

[start]
cmd = "php artisan migrate --force && php -S 0.0.0.0:$PORT -t public"